{{>Layouts/header}}
<form class="container" action="/refri/save" method="post">

    <div class="mb-3">
        회원가입하기<br><br>
        <label class="form-label">사용 할 이메일</label>
        <input type="email" class="form-control" name="email">
    </div>
    <div class="mb-3">
        <label class="form-label">사용 할 비밀번호</label>
        <input type="password" class="form-control" name="password" />
    </div>
    <button type="submit" class="btn btn-primary">회원가입</button>

</form>
{{>Layouts/footer}}